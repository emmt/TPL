# TLP plugin for Yorick

Copy `Makefile.in` as `Makefile`, run:

```.sh
yorick -batch make.i
```

edit settings in the beginning of `Makefile`, then build and (optionally)
install:

```.sh
make clean
make
make install
```
